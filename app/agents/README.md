# app.agents compatibility shims

`adaad.agents` is the canonical namespace for agent implementation modules.

This package remains only to preserve backward compatibility for imports that
still reference `app.agents.*` during migration. Do not add new implementation
logic here.
